node_modules/
dist/
.vscode/
.DS_Store
*.log
.vercel

# Bien moi truong: chan moi file .env THAT (chua mat khau SAP/Gmail),
# nhung VAN commit .env.example de nguoi moi biet can dien nhung gi.
.env
.env.*
!.env.example

# Runtime state (seed lại tự động bởi server.js nếu thiếu — không nên track theo git,
# vì mỗi lần chạy/test local sẽ đổi nội dung và gây conflict giữa các thành viên)
data/approvals.json
data/notifications.json
